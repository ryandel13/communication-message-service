server:
  port: 8804

spring:
  application:
    name: communication-message-service
  cloud:
    config:
      discovery:
        enabled: true
      username: user
      password: password

eureka:
 client:
  serviceUrl:
   defaultZone: http://localhost:8761/eureka/
 healthcheck:
   enabled: true